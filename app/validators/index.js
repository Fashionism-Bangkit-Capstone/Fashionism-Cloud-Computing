const authValidator = require('./auth.validator');
const profileValidator = require('./profile.validator');
const preferenceValidator = require('./preference.validator');

module.exports = {
  authValidator,
  profileValidator,
  preferenceValidator,
};
